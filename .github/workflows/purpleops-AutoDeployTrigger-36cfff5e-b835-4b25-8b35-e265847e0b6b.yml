name: Trigger auto deployment for purpleops

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/purpleops-AutoDeployTrigger-36cfff5e-b835-4b25-8b35-e265847e0b6b.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.PURPLEOPS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.PURPLEOPS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.PURPLEOPS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.PURPLEOPS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.PURPLEOPS_REGISTRY_PASSWORD }}
          containerAppName: purpleops
          resourceGroup: NetworkWatcherRG
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


